# Used when livedemo-proxy Root Directory = "proxy" (common for monorepo services).
# Docker context is this directory; see proxy/Dockerfile.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
dockerContext = "."

[deploy]
restartPolicyType = "ON_FAILURE"
