# Copy to `backend.override.env` (gitignored) and fill for local Docker.
# Docker Compose can be extended to use: env_file: [envs/backend.env, envs/backend.override.env]
OPENAI_API_KEY=
STRIPE_SECRET_KEY=
