# =============================================================
# railway/backend.railway.toml
# LiveDemo Backend API — internal only, no public domain
# =============================================================

[build]
  # Patched image: S3 bucket livedemo-cdn → dk-livedemo-cdn (see backend-patch/)
  dockerImage = "ghcr.io/matty-1337/dk-livedemo-backend:v1"

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3

[deploy.healthcheck]
  path = "/health"
  timeout = 30
